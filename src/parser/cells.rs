use crate::{Cells, Error, Extras, Parser, PuzzleGrid, Result, parse_string};

impl<'a> Parser<'a> {
    pub(crate) fn parse_cells(
        &mut self,
        grid: &'a PuzzleGrid,
        extras: &'a Extras,
    ) -> Result<Cells> {
        todo!()
    }
}
